# a small Forth for ARM Cortex M4

- install `qemu-system-arm`
- `make` to build .bin and .lst
- `gdb` to attach to remote server (see .gdbinit)

## Features

- outputs 1 character to the serial console
